sudo: required

language: python

python:
  - '3.5'

services:
  - docker

before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script:
  - cp dataactcore/config_example.yml dataactcore/config.yml
  - docker-compose -f docker-compose.integrationtest.yml up -d
  - pytest --cov=. --cov-report term --cov-report xml:tests/coverage.xml --junitxml=tests/test-results.xml
  - docker-compose -f docker-compose.integrationtest.yml down

after_script:
  - mv tests/coverage.xml coverage.xml
  - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT
